package com.fixy.backend.dto;

/** Conteo de ofertas vigentes — barato de calcular ahora, listo para el
 * futuro flag del tab (roadmap Historia 3.3, regla ≥30 ofertas vigentes). */
public record OfferPublicCountResponse(long count) {
}
